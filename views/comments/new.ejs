<% include ../partials/header %>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-lg-12 text-center">
            <h1>Adicionar Novo Comentário para <%= campground.name %></h1>
        </div>
        <div class="col-sm-12 col-md-5">
            <form action="/campgrounds/<%= campground._id %>/comments" method="POST">
                <div class="form-group">
                    <input class="form-control" type="text" name="comment[text]" placeholder="Texto"></input>
                </div>
                <div class="form-group">
                    <button class="btn btn-success btn-lg btn-block">Criar Comentário</button>
                </div>
            </form>
            <a class="btn btn-danger btn-sm" href="/campgrounds/<%= campground._id %>">Voltar</a>
         </div>
    </div>
</div>

<% include ../partials/footer %>